# elci
A lambda calculus interpreter with a REPL and extended functionality i.e. Libraries, keywords, variables(constants) but still have the most in the form of pure functions as possible. Also, "type instances" should be defined as automatic procedures, generating the functions representing the data.
